package org.enumeration;

//The main method inside the Enum
public enum Color {
	RED, BLUE, GREEN, WHITE, YELLOW, BLACK, ORANGE;

	public static void main(String[] args) {
		Color c = Color.BLACK;
		System.out.println(c);
	}
}
